{
  "name": "grocery-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "grocery-agent code repo: the grocery-mcp Worker plus the index/site build tooling that data repos run via reusable CI workflows.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:indexes": "node scripts/build-indexes.mjs",
    "build:site": "node scripts/build-site.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^16.4.2",
    "smol-toml": "^1.3.1"
  }
}

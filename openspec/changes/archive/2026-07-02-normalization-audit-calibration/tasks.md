# Tasks — normalization-audit-calibration

Implementation notes that apply throughout: `packages/worker/src/corpus-db.ts` contains an intentional NUL byte (`\x00` is the pair-key separator in `filterCommittableEdges`) — **patch it via a python/node script, never Edit/grep** (they treat the file as binary). All new job-summary fields are additive; no `job_health` name changes, no `src/index.ts` wiring changes (comment updates only, if any). Constants introduced: `NORMALIZE_CORESOLVE_REJECT_BACKOFF_MS = 30 * 24 * 60 * 60 * 1000` and `SEGMENT_REPAIR_MAX_PER_TICK = 5` in `src/ingredient-normalize.ts`; `EDGE_REPLAY_MAX_PER_TICK = 10` and `STRUCTURAL_RESTORE_MAX_PER_TICK = 20` in `src/ingredient-edge-audit.ts`.

## 1. Schema + shared data layer (corpus-db)

- [x] 1.1 Add `packages/worker/migrations/d1/0036_coresolution_rejections.sql`: `CREATE TABLE ingredient_coresolution_rejection (a TEXT NOT NULL, b TEXT NOT NULL, decided_at INTEGER NOT NULL, PRIMARY KEY (a, b));` with a header comment naming this change and the pecorino/parmesan defect (`(a, b)` = the pair's surviving ids, lexicographically ordered, at decision time)
- [x] 1.2 In `src/corpus-db.ts` (script patch): extend `NormalizationLog.outcome` union with `"edge_restore"`; add reader `readCoResolutionRejections(env): Promise<{ a: string; b: string; decided_at: number }[]>` and writer `upsertCoResolutionRejection(env, a, b, now)` (`INSERT ... ON CONFLICT(a, b) DO UPDATE SET decided_at = excluded.decided_at`)
- [x] 1.3 In `src/corpus-db.ts`: add `repairSegmentOverflow(env, plan: { overflow: string; prefix: string; shape: "merge" | "reroot" | "mint" ; prefixNode?: { base: string; detail: string; search_term: string; concrete: boolean } })` executing one `d.batch` per design D3 — shape `merge` delegates to the same UPDATE+log statements as `mergeIdentities` (or the pass calls `mergeIdentities` directly for that shape and this writer handles only `reroot`/`mint`; pick one and keep the cycle-guard semantics), shape `reroot` = `UPDATE ingredient_identity SET representative = NULL WHERE id = ?prefix` + `UPDATE ingredient_identity SET representative = ?prefix WHERE id = ?overflow` + log row (`outcome: "merge"`, `term: overflow`, `resolved_id: prefix`, `detail: { note: "segment_overflow", reroot: true }`), shape `mint` = `INSERT OR IGNORE` prefix node (embedding NULL, source `auto`, `decided_at` now) + the overflow UPDATE + log row (`detail: { note: "segment_overflow", minted_prefix: true }`)
- [x] 1.4 In `src/corpus-db.ts`: add `insertAuditedEdge(env, from, to, kind)` (`INSERT OR IGNORE INTO ingredient_edge ... audited_at = now`, optionally batched with an `INSERT OR IGNORE` base-node mint via an `opts.mintBase?: { id: string }` parameter) and `readUnreplayedEdgeDrops(env, limit): Promise<{ id: number; term: string; detail: unknown }[]>` (full read of `outcome='edge_drop'` rows ordered by `id`, JS-parse `detail`, filter rows lacking `replayed_at`, slice to `limit`) and `markEdgeDropReplayed(env, id, patch: Record<string, unknown>)` (read the row's detail, merge `{ replayed_at, ...patch }`, `UPDATE ingredient_normalization_log SET detail = ?2 WHERE id = ?1`)
- [x] 1.5 In `src/corpus-db.ts`: born-mark new edge-drop rows and structure edge logs — everywhere the edge audit writes `edge_drop`/`edge_keep`/`edge_restore` details the log detail gains `from`/`to`/`kind` structured fields, and `edge_drop` details gain `replayed_at` at write time (design D7; the actual detail objects are built in `src/ingredient-edge-audit.ts` — this task is only the SCHEMAS.md-visible convention, verify no corpus-db change is needed beyond the outcome union)
- [x] 1.6 Extend `test/fake-d1.ts` only if needed: `UPDATE ingredient_normalization_log SET detail = ? WHERE id = ?` (single-row UPDATE by integer id) and the `ingredient_coresolution_rejection` upsert shape; keep additions minimal and shape-driven like the 0035 round
- [x] 1.7 Extend `test/corpus-db.test.ts`: `upsertCoResolutionRejection` insert + refresh semantics; `repairSegmentOverflow` reroot batch (prefix representative cleared, overflow pointed, log row), mint batch (INSERT OR IGNORE + pointer + log); `insertAuditedEdge` born-stamped + ignore-on-duplicate; `readUnreplayedEdgeDrops` filters marked rows and respects `limit`; `markEdgeDropReplayed` merges without clobbering existing detail fields

## 2. Classifier recalibration (`src/ingredient-classify.ts`)

- [x] 2.1 Rewrite `DIRECTION_SYSTEM_PROMPT` per design D6: satisfies = "a shopper who has FROM can acceptably fulfill a recipe or shopping request for TO" (explicitly not identical-product); membership rule (member fulfills the category concept, never the reverse); whole→derived retained; distinct-products rule narrowed to same-level specific products with the explicit "do not use this rule to deny specific→general/category or whole→derived directions" instruction
- [x] 2.2 Extend `DIRECTION_FEW_SHOT` (keep the existing four): `honey raisins`→`raisins` = forward ("honey-coated raisins are still raisins"); `sweet maui mango habanero sauce`→`hot sauces (various)` = forward (membership); `fruit pectin`→`jellies and jams (various)` = neither (an ingredient for making the product is not the product); `frozen fruit mix`→`dried fruit blend` = neither (different preservation state)
- [x] 2.3 In the identity-confirm `SYSTEM_PROMPT` add the rule "A term that differs from a candidate id only by punctuation, pluralization, or word order is the SAME product — pick same", and add one `FEW_SHOT` entry: term `"salmon fillets skin-on"`, candidates including `{"id":"salmon fillets, skin-on","similarity":0.98}` → `{ outcome: "same", match: "salmon fillets, skin-on", ... }`
- [x] 2.4 Extend `test/ingredient-normalize.live.test.ts` (live-gated, not CI-blocking) with recalibrated direction fixtures: `honey raisins→raisins` forward, `sweet maui mango habanero sauce→hot sauces (various)` forward, `whole cardamom pods→ground cardamom` forward, `semolina flour→all-purpose flour` neither, `fruit pectin→jellies and jams (various)` neither, `ground nutmeg→whole nutmeg` reverse; and an identity-confirm punctuation fixture (`salmon fillets skin-on` vs the comma candidate → same)

## 3. Capture job (`src/ingredient-normalize.ts`)

- [x] 3.1 Segment guard in `buildResolution`: when `confirm.outcome === "specialization" && confirm.match && confirm.match.includes("::")`, return the SAME-shaped resolution on `confirm.match` (no `node`, edges via `mapEdges(confirm.edges, confirm.match)`, log `outcome: "same"`, detail `{ reason: confirm.reason, note: "specialization_demoted", proposed_detail: confirm.detail }`)
- [x] 3.2 Lexical fast path: add `lexicalKey(s)` (lowercase; replace `/[^a-z0-9:]+/g` with a space; collapse whitespace; trim) and a map builder over surviving node ids + alias variants → representative-resolved survivor, skipping ambiguous keys (two distinct survivors); in `resolveOne`, before the floor check, a unique hit returns a SAME resolution (`{ id: hit, edges: [], log: { outcome: "same", resolved_id: hit, model: null, detail: { note: "lexical_match" } } }`); new deps `identitySources()` (→ `readIdentitySources`) and `aliasTargets()` (→ `readAliasTargets`) wired in `buildNormalizeDeps`; `NormalizeSummary` += `lexical`
- [x] 3.3 Segment-overflow sub-pass `reconcileSegmentOverflow(deps, summary)` run in `reconcileNormalization` before `reconcileCoResolution`, every tick (also on an empty queue): from `identitySources()`, select surviving (`representative` null) `source='auto'` nodes with `id.split("::").length > 2`, capped at `SEGMENT_REPAIR_MAX_PER_TICK`; for each compute `prefix = id.split("::").slice(0, 2).join("::")` and dispatch per design D3 (prefix absent → `mint`; `resolveVia(prefix) === overflow` → `reroot`; else → `merge` via `mergeIdentities`); human overflow nodes skipped; new dep `repairOverflow(plan)` (→ `repairSegmentOverflow`); `NormalizeSummary` += `segmentRepaired` (+ `segmentSkipped` for the counted human-skip)
- [x] 3.4 Rejection memory in `reconcileCoResolution`: new deps `mergeRejections()` (→ `readCoResolutionRejections`) and `rememberRejection(a, b, now)` (→ `upsertCoResolutionRejection`); after reading pairs, drop any pair whose ordered surviving-id key has `now - decided_at < NORMALIZE_CORESOLVE_REJECT_BACKOFF_MS`, counting `mergeSuppressed`; in the existing `confirm.outcome !== "same" || confirm.match !== pair.b` branch, call `rememberRejection(min(a,b), max(a,b), now)` before counting `mergeRejected`; transient-confirm and both-human paths record nothing
- [x] 3.5 Tests in `test/ingredient-normalize.test.ts` (extend the existing dep-injected harness):
  - `buildResolution demotes a specialization on a detailed match to SAME` — exact sockeye reproduction: match `salmon fillets, skin-on::species-atlantic-sockeye`, detail `species-atlantic-sockeye` → resolution id equals the match, no node, log notes `specialization_demoted`
  - `buildResolution still specializes a bare base` (regression: `ground beef` + `fat-80-20`)
  - `lexical fast path resolves a punctuation variant with no confirm call` (term `salmon fillets skin-on`, surviving id `salmon fillets, skin-on`; assert `confirm` never invoked, summary.lexical === 1) and `lexical fast path skips an ambiguous key`
  - `segment overflow reroots when the prefix resolves to the overflow` — fixture mirrors production (prefix.representative = overflow) → asserts a `reroot` plan; `segment overflow merges when the prefix survives elsewhere`; `segment overflow mints a missing prefix`; `segment overflow skips human nodes and quiesces when none exceed two segments` (second run plans nothing)
  - `co-resolution suppresses a remembered rejection within backoff` (pecorino/parmesan fixture: first tick confirms + records, second tick makes no confirm call, summary.mergeSuppressed === 1), `re-proposes after backoff`, `re-proposes when a survivor changes` (merge one side, key no longer matches), `transient confirm failure records no rejection`

## 4. Alias re-audit (`src/ingredient-alias-audit.ts`)

- [x] 4.1 No-op keep guard in `decide()`: before calling `buildResolution`, when `confirm.outcome === "novel"`, compute `c = validateCanonicalId(confirm.canonical)` (import from `ingredient-normalize.js`); if `c` and `resolveVia(ctx.rep, c) === previous`, return the keep resolution (`{ term: row.variant, id: previous, edges: [], log: { ..., outcome: "novel", resolved_id: previous, detail: { audit: "alias", previous_id: previous, note: "canonical_is_standing" } } }`) — requires threading `ctx.rep` (or a `resolvePrev` callback) into `decide()`
- [x] 4.2 Lexical fast path in `auditOne`: before the confirm call, a unique `lexicalKey` hit (same helper as 3.2, map built from the pass's existing `ctx` reads) commits a SAME resolution onto the hit branded with `brandAudit(..., previous)` and `detail.note = "lexical_match"`, spending no confirm call; counted `kept` when the hit resolves to `previous`, else `repointed`
- [x] 4.3 Tests in `test/ingredient-alias-audit.test.ts`:
  - `a specialization onto the already-detailed standing match is kept, not deepened` — full sockeye reproduction: alias `atlantic sockeye salmon fillets` → node `salmon fillets, skin-on::species-atlantic-sockeye`, confirm returns SPECIALIZATION(match = that id, detail `species-atlantic-sockeye`) → assert committed id === standing id, no 3-segment id anywhere, summary counts `kept`, row stamped
  - `a novel canonical equal to the standing id is a keep, not a verbatim mint` (confirm NOVEL, canonical = standing id → no node minted, id === previous, log notes `canonical_is_standing`)
  - `a punctuation-variant alias resolves lexically with no confirm call`
- [x] 4.4 Confirm no behavior change to the existing kept/repointed/orphan-merge paths (existing tests stay green unmodified)

## 5. Edge re-audit (`src/ingredient-edge-audit.ts`)

- [x] 5.1 Add exported `isStructuralEdge(from: string, to: string): boolean` (`from.split("::").length === 2 && from.startsWith(to + "::")` with `to` containing no `::`); in `auditEdges`' drain, immediately after the self-loop branch, exempt a structural edge whose from-node survives (`resolve(e.from_id) === e.from_id`): `stamp` + `log(edgeLog(e, "edge_keep", null, { note: "structural" }))`, `summary.structural++`, no model call; in the 2-cycle branch, a structural reverse side is never deleted — it is stamped + kept deterministically regardless of the verdict
- [x] 5.2 Structural pre-pass `ensureStructuralEdges` run at the top of `auditEdges` every tick (move the identities/edges loads above the empty-batch early-return): (a) sweep — delete every `source='auto'` edge whose resolved endpoints coincide, stamped or not, logging `edge_drop { note: "self_loop", from, to, kind, replayed_at }`, `summary.self_loops_swept++`; (b) guarantee — for each surviving 2-segment node `X::d` with no live edge of any kind `(X::d, X, *)`, call `insertAuditedEdge(X::d, X, "general")` (minting base `X` when absent per 1.4) + log `edge_restore { note: "structural_guarantee", from, to, kind }`, `summary.structural_restored++`; cap writes at `STRUCTURAL_RESTORE_MAX_PER_TICK`; new deps `insertEdge` (→ `insertAuditedEdge`) wired in `buildEdgeAuditDeps`
- [x] 5.3 Replay sub-pass `replayEdgeDrops` run after the drain: new deps `unreplayedDrops(limit)` (→ `readUnreplayedEdgeDrops`) and `markReplayed(id, patch)` (→ `markEdgeDropReplayed`); per design D7 — strict parse `/^(.+) -\[(general|containment|membership)\]-> (.+)$/`; mark-only paths (`unparseable`, deterministic `self_loop`/`human_reverse` notes, `structural` with surviving from, `endpoint_merged`); then split on whether a resolved reverse edge exists in the live edge set:
  - **no standing reverse** → one `checkDirection(readable(resolve(from)), readable(resolve(to)))`: on `forward|both` insert via `insertEdge`, log `edge_restore { audit: "edge", replay_of: id, direction, reason, from, to, kind }`, mark `{ replay: "restored", direction }`; on `reverse|neither` mark `{ replay: "stands", direction, reason }`
  - **standing reverse (pair re-decision — architect amendment, never withhold)**: reverse `source='human'` → mark `{ replay: "human_reverse_standing" }`, no model call, no restore; reverse structural (`isStructuralEdge` + surviving from) → mark `{ replay: "structural_reverse" }`, no model call, no restore, reverse untouched; otherwise ONE `checkDirection` call decides the pair — `forward` → `insertEdge` the dropped edge + `deleteEdge` the standing reverse + log `edge_drop { note: "replay_cycle", replay_of: id, from, to, kind, replayed_at }` for it (even when it carries an earlier keep stamp) + log `edge_restore` + mark `{ replay: "restored", direction, cycle: true }`; `both` → restore, keep the reverse; `reverse` → mark `{ replay: "stands", direction, reason }`, reverse untouched; `neither` → mark `{ replay: "stands", direction, reason }` + `deleteEdge` the reverse (+ its `replay_cycle` drop log)
  - contract-invalid → mark `{ replay: "confirm_failed_safe" }`, no insert, no reverse deletion; transient → leave unmarked (rethrow to the per-row catch); bound `EDGE_REPLAY_MAX_PER_TICK`; `summary.replayed++` per marked row, `summary.restored++` per insert (pair-deleted reverses count under the existing `dropped`)
- [x] 5.4 Born-mark + structure all drop/keep logs written by the drain: every `edgeLog(...)` detail gains `from: e.from_id, to: e.to_id, kind: e.kind`, and `edge_drop` details gain `replayed_at: now`
- [x] 5.5 `EdgeAuditSummary` += `structural`, `structural_restored`, `self_loops_swept`, `replayed`, `restored` (existing fields untouched)
- [x] 5.6 Tests in `test/ingredient-edge-audit.test.ts`:
  - `a structural edge is kept deterministically with no model call` (rotel fixture; assert `checkDirection` never invoked) and `a 2-cycle verdict never deletes a structural reverse side`
  - `a structural-shaped edge from a merged-away node is not exempt` (fish-sauce fixture: from-node representative set → falls through to resolved-endpoint handling)
  - `the guarantee restores a missing structural edge born-stamped` (assert `insertEdge` called with `general`, `edge_restore` logged) / `mints a missing base node` / `is idempotent` (second run plans nothing) / `respects the write cap`
  - `the sweep deletes a stamped rep-resolved self-loop` (post-repair salmon fixture: born-stamped structural edge whose endpoints now resolve together)
  - `replay restores a wrongly-dropped edge under the recalibrated verdict` (honey-raisins fixture: drop row, `checkDirection` → forward, edge inserted born-stamped, `edge_restore` logged with `replay_of`, row marked)
  - `replay marks deterministic and dead rows without model calls` (self_loop note, human_reverse note, structural-live, endpoint-merged, unparseable term — five rows, zero `checkDirection` calls)
  - `replay re-decides a pair when the reverse edge stands` (whole-frozen-chicken fixture: drop row for `whole frozen chicken -[containment]-> chicken tenderloin` + standing stamped reverse `chicken tenderloin -[general]-> whole frozen chicken`; `checkDirection` → forward → dropped edge restored born-stamped, reverse DELETED with a `replay_cycle` drop log carrying `replay_of`, one model call total, no 2-cycle remains)
  - `pair re-decision verdict matrix` (`both` → restore + reverse kept; `reverse` → stands + reverse kept; `neither` → stands + reverse deleted)
  - `a human standing reverse wins deterministically in replay` (no `checkDirection` call, no restore, reverse untouched, mark `human_reverse_standing`) and `a structural standing reverse blocks the restore` (no call, mark `structural_reverse`)
  - `a contract-invalid pair check deletes nothing` (mark `confirm_failed_safe`, reverse untouched)
  - `replay is idempotent and self-quiescing` (marked rows never re-selected; a transient failure leaves the row unmarked and a second tick resumes it; born-marked new drops are never selected)
  - `summary shape is additive` (existing fields still produced)

## 6. Docs (lockstep, current-state wording — no history narration)

- [x] 6.1 `docs/SCHEMAS.md`: add `ingredient_coresolution_rejection` to the shared-corpus D1 section (columns, ordered-survivor key, backoff semantics); extend the `ingredient_normalization_log` notes with the `edge_restore` outcome, the `replayed_at`/`replay` detail marks on `edge_drop` rows, and the structured `from`/`to`/`kind` fields on edge decision rows
- [x] 6.2 `docs/ARCHITECTURE.md` (ingredient-normalization capture section): state the one-detail-segment id bound and the specialization demotion; the segment-overflow repair; the lexical-identity fast path; the co-resolution rejection memory; the structural-edge exemption + guarantee and the "acceptably fulfills" direction semantics; the one-shot drop replay — as how the system works today
- [x] 6.3 Verify `docs/TOOLS.md` needs no change (no tool params/returns touched) and `src/normalize-admin.ts` needs no change (the `startsWith("edge_")` filter already excludes `edge_restore`)

## 7. Verification

- [x] 7.1 `aubr typecheck` and the FULL `aubr test` suite green; `aubr test:tooling` untouched but run once
- [x] 7.2 `npx --yes openspec validate normalization-audit-calibration --strict` passes
- [ ] 7.3 Post-deploy production acceptance (remote D1, read-only; run after two ticks, ~10 min):
  - `SELECT id, representative FROM ingredient_identity WHERE id LIKE '%::%::%'` → the 3-segment node's `representative` = `salmon fillets, skin-on::species-atlantic-sockeye`, and that prefix's `representative` IS NULL
  - `SELECT from_id, to_id, audited_at FROM ingredient_edge WHERE from_id IN ('rotel (original)::heat-mild','snacking pickles::form-chips','serrano or jalapeño peppers::form-diced','grits::cook-time-minute','garlic::form-jarred')` → all five structural edges present, `audited_at` NOT NULL
  - `SELECT COUNT(*) FROM ingredient_normalization_log WHERE outcome='edge_drop' AND (detail IS NULL OR detail NOT LIKE '%replayed_at%')` → 0 after ≤5 ticks; `SELECT term FROM ingredient_normalization_log WHERE outcome='edge_restore'` includes the honey-raisins/habanero-class restores
  - Pair re-decision converged the mislabel casualty: `SELECT from_id, to_id FROM ingredient_edge WHERE (from_id='whole frozen chicken' AND to_id='chicken tenderloin') OR (from_id='chicken tenderloin' AND to_id='whole frozen chicken')` → only the whole→tenderloin direction remains (the wrongly-kept reverse deleted, its `replay_cycle` drop logged)
  - `SELECT * FROM ingredient_coresolution_rejection` → one row (pecorino/parmesan pair, ordered); the latest `ingredient-normalize` job_health summary shows `mergeRejected: 0` and `mergeSuppressed: 1`
  - `SELECT from_id FROM ingredient_edge WHERE from_id LIKE 'salmon fillets, skin-on::species-atlantic-sockeye::%'` → 0 rows (the stamped self-loop was swept)
